# This file can be left empty or used for project-wide configurations
# Ensures Python treats the directory as a package
import os
import sys

# Optional: Add project root to Python path
sys.path.append(os.path.dirname(os.path.abspath(__file__)))
